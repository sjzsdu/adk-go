1. **实现端口可用性检测函数**：

   * 添加`isPortAvailable`函数，使用`net.Listen`尝试绑定端口，检测端口是否可用

2. **修改Run方法实现端口自动重试**：

   * 在创建HTTP服务器前，循环检测端口可用性

   * 如果端口被占用，自动递增端口号并重新检测

   * 限制最大重试次数（如100次），避免无限循环

3. **添加浏览器自动打开功能**：

   * 添加`openBrowser`函数，使用`os/exec`包调用系统命令打开浏览器

   * 支持不同操作系统（macOS、Windows、Linux）

4. **更新配置和日志**：

   * 确保使用实际绑定的端口更新`webUrl`

   * 在日志中显示实际使用的端口号

   * 在服务器成功启动后调用`openBrowser`函数

5. **添加必要的导入包**：

   * `net`：用于端口检测

   * `os/exec`：用于打开浏览器

   * `runtime`：用于检测操作系统类型

6. **测试和验证**：

   * 确保端口冲突时能自动重试

   * 确保浏览器能自动打开

   * 确保日志显示正确的端口号

